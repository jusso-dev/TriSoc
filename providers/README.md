# Provider collectors

Provider packages will be added one reviewed phase at a time: Microsoft Sentinel,
AWS-native security operations, then Google Security Operations. Each collector
must use an official SDK as its primary interface, paginate and rate-limit calls,
return typed permission/licence/API errors, and ship sanitised contract fixtures.

