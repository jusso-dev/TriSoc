# Applications

Phase 1 exposes the CLI and MCP adapters from `cmd/trisoc`. The Go REST API and
worker arrive with provider assessments and scheduling; the Next.js 16 web
application arrives in phase 6. Applications consume shared domain services and
do not duplicate attestation logic.

